export type Book = {
    bookId: string
    title: string
    author: string
    price: number
    description: string
    image: string
    alt: string
    availableQuantity: number;
    totalQuantity: number;
}